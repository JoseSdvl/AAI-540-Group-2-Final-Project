# Core
numpy>=1.26,<2.0
pandas>=2.1,<2.3
scikit-learn>=1.2,<1.3
xgboost>=2.0,<2.2
joblib>=1.3
pyarrow>=15.0
# NOTE: scikit-learn pinned to 1.2.x to match the SageMaker sklearn inference
# container (1.2-1). Newer sklearn versions (1.4+) add internal classes like
# `_RemainderColsList` that don't exist in 1.2, causing pickle unpickling
# errors at inference time. Training must match the inference environment.
# NOTE: numpy pinned to <2.0 because scikit-learn 1.2.x wheels are compiled
# against the NumPy 1.x ABI. Running them under NumPy 2.x raises
# `module 'sklearn.utils._openmp_helpers' has no attribute '__pyx_capi__'`.

# AWS / SageMaker
# NOTE: We pin the CLASSIC SageMaker Python SDK (v2.x). Some newer Studio
# kernels ship only `sagemaker-core` (the modular v3 rewrite), which owns
# the `sagemaker` namespace but does NOT provide `sagemaker.session`,
# `sagemaker.feature_store`, `sagemaker.get_execution_role`, etc.
# Installing this pin restores the classic surface our code targets.
boto3>=1.34
sagemaker>=2.220,<3.0
s3fs>=2024.3.0

# Evaluation / plotting (notebook only)
matplotlib>=3.8
seaborn>=0.13

# Dev / CI
pytest>=8.0
pytest-cov>=5.0
ruff>=0.5
black>=24.4
